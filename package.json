{
  "name": "zu-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "zu のデータ・作図ルール・画面ロジックを点検するテスト（配信物はビルドなしのまま。テストだけ dev 依存を持つ）。",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
